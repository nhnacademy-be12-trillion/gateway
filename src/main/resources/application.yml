# Config Server 연결을 위한 최소한의 설정
spring:
  config:
    import: optional:configserver:${CONFIG_SERVER_URI:http://localhost:10418}
  cloud:
    config:
      username: be12-team1
      password: nhnacademy123!
  application:
    name: gateway

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL:http://be12-team1:nhnacademy123!@localhost:8761/eureka}
##---------------------------------------------------------------------------------------------------------
uri:
  service:
    auth: lb://auth-service
    member: lb://member-service
    book: lb://book-service
    order: lb://order-service
    coupon: lb://coupon-service
    search: lb://search-service
