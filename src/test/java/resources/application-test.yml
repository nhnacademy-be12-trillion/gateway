# src/test/resources/application-test.yml
spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false       # Config Client 비활성화
    discovery:
      enabled: false       # Eureka Client 연결 시도 비활성화 (선택적)

eureka:
  client:
    enabled: false